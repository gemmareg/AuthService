namespace AuthService.Shared
{
    public static class Enums
    {
        public enum TokenType
        {
            Access,
            Refresh,
            EmailConfirmation,
            PasswordReset,
        }
    }
}
